import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    background: 'linear-gradient(0deg, rgba(34,103,195,1) 0%, rgba(253,81,45,1) 100%)',
    padding: '50px',
    height: "100%",
    minHeight: "100vh",
    width: "100%",
    color: "#ffffff",
  },
});
